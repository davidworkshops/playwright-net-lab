pipeline {
    agent any

    stages {

        stage('Environment') {
            steps {
                sh 'pwd'
                sh 'whoami'
                sh 'uname -a'
                sh 'ls'
            }
        }

    }
}